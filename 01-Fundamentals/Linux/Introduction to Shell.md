
# **Terminal**
![[Pasted image 20250909174832.png]]
A **terminal** is a **program that provides a text interface** to interact with the system. THe terminal itself not execute commands, it just send them to a shell

# Shell
Sheel or command line provides a text-based input/output interface between users and the kernel for a computer system. 
A shell us a program that interprets commands. It reads commands, executes programs, and returns the output.

![[Pasted image 20250909174241.png]]

User types commands
       │
      ▼
Terminal (interface)
       │
      ▼
 Shell (command interpreter)
       │
      ▼
 Kernel (executes system calls)

___
**Previous:** [[Linux Structure]]
**Next:** [[Workflow]]
___
